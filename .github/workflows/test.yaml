name: test
on:
  pull_request: {}
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    - name: Install uv
      uses: astral-sh/setup-uv@v8.2.0
    - run: uv sync --frozen
      shell: bash
    - run: make lint
      shell: bash
    - run: make test
      shell: bash
    - run: make build
      shell: bash
