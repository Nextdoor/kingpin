simplejson

# General App Requirements
tornado==3.1.1
boto==2.32.1

# Used to make synchronous tasks asynchronous
futures

# Used to parse out the JSON and validate its format.
jsonschema
commentjson

# Explicit version and source set in the setup.py
simplejson
requests
python-rightscale

# Note: This is only installable directly from Github, but because
# this file is parsed by setup.py, we cannot use the pip+github
# syntax. Instead, the Makefile handles the installation of this
# dependency.
tornadomail
