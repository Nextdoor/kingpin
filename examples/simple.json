{ "desc": "main stage",
  "actor": "group.Sync",
  "options" : { "acts": [

    { "desc": "stage 1", "actor": "group.Sync", "options": { "acts": [
      { "desc": "copy serverA", "actor": "copy", "options": { "source": "template", "dest": "serverA" } }
    ] } },

    { "desc": "stage 2", "actor": "group.Sync", "options": { "acts": [
      { "desc": "copy serverB", "actor": "copy", "options": { "source": "template", "dest": "serverB" } }
    ] } },

    { "desc": "stage 3", "actor": "group.Sync", "options": { "acts": [
      { "desc": "copy serverC", "actor": "copy", "options": { "source": "template", "dest": "serverC" } }
    ] } },
  ]
}
